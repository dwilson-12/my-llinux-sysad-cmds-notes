LINUX ACCOUNT AUTHENTICATION

Types of Accounts
1. Local accounts		= eg. useradd, usermod -G groupname username
2. Domain/Directory accounts	= what if you have to create a user account to thousands of servers? you have to have a domain.

IN WINDOWS = ACTIVE DIRECTORY

LDAP is a protocol. It is used only for windows and mac. Authenticate against a directory. 